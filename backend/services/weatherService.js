exports.getWeatherScore = async (location) => {
    // Stub definition for Weather logic
    // A fully scaled weather service would ping OpenMeteo for 7-day trailing data.
    // We return a high baseline assuming optimal flying conditions.
    return 85;
};
